import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const commentsSlice = createSlice({
  name: "commentsSlice",
  initialState,
  reducers: {},
});

export default commentsSlice;
